---
export interface Props {
  filename: string;
}

const { filename } = Astro.props as Props;
const { default: innerHTML } = await import(/* @vite-ignore */ `../svg/${filename}.svg?raw`);
---

<Fragment set:html={innerHTML} />